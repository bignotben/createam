NEXT_PUBLIC_CMS_URL=http://localhost:3001
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_WEB3FORMS_ACCESS_KEY=YOUR_ACCESS_KEY_HERE

# "Kako vas AI vidi?" tool — public URL of the deployed Cloudflare Worker
# (see /worker/README.md). Local dev default matches `wrangler dev`'s
# default port; replace with the real workers.dev (or custom domain) URL
# before deploying, and rebuild (this is baked in at `npm run build` time).
NEXT_PUBLIC_AI_WORKER_URL=http://localhost:8787
