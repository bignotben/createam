#!/usr/bin/env bash
# Builds the static web/ site from CMS content.
# Filled in during Phase 12 (Build & deploy scripts) per createam-build-spec.md §8.
set -euo pipefail

echo "TODO: verify Payload (cms/) is reachable"
echo "TODO: cd web && next build (output: 'export' -> web/out)"
